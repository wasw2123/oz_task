### 1. DB 의 종류(Postgresql, MySQL, MongoDB 등)와 각 종류에 대한 특징을 이해하고 내 서비스에 적절한 데이터베이스를 선택할 수 있다.
몽고디비는 nosql로 유연한 스키마로 데이터를 저장할 수 있는 특징이 있고 *자유로운 데이터구조     
mysql은 관계형 중 읽기 성능이 뛰어나고 *많은 자료에서의 안정적 속도     
postgre는 객체 관계형 db라 nosql처럼 쓸수도 있는 특징, 복잡한 쿼리에 적합 *정확한 데이터      

### 2. sqlalchemy, tortoise ORM 등 요구사항에 적절한 ORM을 선택하여 활용할 수 있다.
알케미는 1.4부터 비동기가 가능해짐, 반면 토터스는 비동기용
알케미 - 코드가 길지만 디테일한 제어가 가능하고 오래 사용돼 관련 지식이 많아 문제 해결에 용의       
토터스 - 코드가 간결하고 쉽게 비동기 처리가 가능

### 3. aerich, alembic 등의 마이그레이션 도구를 활용할 수 있다.
### 4. FastAPI 의 Config 설정을 통해 데이터베이스 설정을 가져오고 적용할 수 있다.
### 5. ORM 을 활용하여 API를 작성할 수 있다.